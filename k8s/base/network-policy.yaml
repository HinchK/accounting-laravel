apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: accounting-erp-network-policy
  namespace: accounting-erp-laravel
  labels:
    app: accounting-erp-laravel
spec:
  podSelector:
    matchLabels:
      app: accounting-erp-laravel
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          name: ingress-nginx
    ports:
    - protocol: TCP
      port: 80
    - protocol: TCP
      port: 8000
  - from:
    - podSelector:
        matchLabels:
          app: accounting-erp-laravel
    ports:
    - protocol: TCP
      port: 8000
    - protocol: TCP
      port: 6379
    - protocol: TCP
      port: 3306
  egress:
  - to:
    - namespaceSelector:
        matchLabels:
          name: kube-system
    ports:
    - protocol: UDP
      port: 53
  - to:
    - podSelector:
        matchLabels:
          app: accounting-erp-laravel
  - to: []
    ports:
    - protocol: TCP
      port: 443
    - protocol: TCP
      port: 80
