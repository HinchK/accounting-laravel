apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../../base

namespace: accounting-erp-dev

patches:
- patch: |-
    - op: replace
      path: /spec/replicas
      value: 1
  target:
    kind: Deployment
    name: accounting-erp-laravel

configMapGenerator:
- name: accounting-erp-config
  behavior: merge
  literals:
  - APP_ENV=development
  - APP_DEBUG=true
  - LOG_LEVEL=debug

images:
- name: ghcr.io/liberusoftware/accounting-erp-laravel
  newTag: develop
